#!/usr/bin/env bash

set -o errexit
set -o pipefail
set -o nounset
# set -o xtrace

echo "--- Watching for changes ..."

./node_modules/.bin/nodemon \
	--delay 300ms \
	--ext js,ts,vue,scss,pug \
	--exec "time ./bin/lint && time ./bin/pretty && time ./bin/test"
